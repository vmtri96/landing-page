export type ContactForm = {
  name: string,
  phoneNumber: string,
  email: string,
  message: string,
};

export type ContactErrors = {
  name: string,
  phoneNumber: string,
  email: string,
  message: string,
};